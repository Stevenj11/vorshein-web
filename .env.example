# Copy this file to .env.local and fill in real values.
# .env.local is gitignored — never commit real keys.

# Powers Vorshein AI with real reasoning (Google Gemini). Get a free key at
# https://aistudio.google.com/apikey — no key set falls back to the
# rule-based demo engine automatically (see src/app/api/concierge/route.ts).
GEMINI_API_KEY=

# Sends an email alert on every new booking (Resend). Get a free key at
# https://resend.com/api-keys — no key set just skips the email silently,
# the reservation is still saved and the WhatsApp handoff still works
# (see src/lib/booking/notifyEmail.ts).
RESEND_API_KEY=
